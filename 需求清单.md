# 司量（Siliang）用户需求清单

## 割り勘（AA 记账）页面
- 记一笔，总额可自定义（不限于均分）
- 支持收入 / 支出两个方向
- 分摊方式：均分 或 每人自定义金额
- 有成员库，付款人 / 参与人从成员里下拉选，不能随便打字
- 自动算「谁该付谁多少」（应收 / 总账）
- 能登记还款（谁还了谁，成员下拉）
- 算「还差多少」（净账 = 应收扣还款），换设备也同步
- 收支日历：月历格子写当天收入(+)／支出(−)金额，带当月合计
- 结算汇总不能被日历挡住

## 积分记录页面
- 记一笔：获得 / 消耗
- 支持延迟到账（付与予定）：pending + 预计日期，不计余额、单独高亮
- 账户余额面板：按积分计划分组看余额 + 待入账
- 日历热力图别遮挡

## 物品使用页面
- 记一件物品：名称、分类、开始时间
- 使用状态：使用中 / 已结束
- 结束时间：已结束时必填、使用中时禁用
- 日历区间热力图，展示使用周期（使用中的延伸到今天）
- 点开不能白屏

## Token 使用页面
- 记一笔：应用场景（必填，最重要）、模型（必填、始终显示）、API Key（可隐藏，默认末 4 位）、日期、输入/输出 tokens（非必填，至少一个>0）、费用
- 按应用场景分组统计，卡片显示各应用模型与消耗
- 日历热力图别遮挡

## 登录页面
- 首次启动创建管理员，并展示 + 保存恢复密钥
- 普通登录（用户名 + 密码）
- 「忘记密码？」→ 凭恢复密钥重置
- 登录态持久化（重启还要登录）

## 管理员页面
- 用户管理：增 / 删、角色切换、改密码
- 恢复密钥：查看 / 复制 / 重新生成

## 设置页面
- 语言切换（中文 / 日本語）

## 统一日历组件（跨页面共用）
- SVG 年度热力图（数值热力 + 周期区间），可按应用筛选、按年切换
- 月历收支组件（格子写金额）
- 所有日历可横向滚动，不遮挡

## 全局
- 配色：浅粉 + 浅绿，高级感
- 插画 + Logo：用 irasutoya 小人
- 本地化：数据格式固定 ja-JP，界面中文 + 可切日文
- 存储 + 同步：本地保存，靠同步文件夹跨设备同步（LWW + 墓碑）
- 打包：Apple Silicon dmg，能双击启动
- 交付前全部自查